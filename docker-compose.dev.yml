services:
    server:
            build: .
            ports:
                - 8081:8080
            command: npm start
            volumes:
                - ./src/:/app/src/
    mongodb:
            image: mongo
            volumes:
                - ./data:/data/db
    client:
            build: ../2104-wns-paris-lama-front/
            ports:
                - 3000:3000
            volumes:
                - ./src/:/app/src/
